import React, { useState, useReducer, useMemo } from "react";
import Keyboard from "react-simple-keyboard";

export const Keeb = () => {
	return (
		<div className="bc-keeb">
			<Keyboard />
        </div>
	);
};
